# files
- 不支持 glob 模式


| 分类 | 属性 | 含义 | 版本 |
| :----- | :-------: | ----- | -------:|
| Emit | noEmit | 不产生任何的文件输出，包括编译代码、sourcemap文件或类型声明；这个属性的主要作用是将编译的工作转交给其他编译工具（Babel、SWC等），而TS只用于类型检查和编辑器智能提示| 1.5