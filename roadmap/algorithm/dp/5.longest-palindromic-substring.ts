/*
 * @lc app=leetcode id=5 lang=typescript
 *
 * [5] Longest Palindromic Substring
 */

// 先理解什么是回文字符串
// acca

// @lc code=start
function longestPalindrome(s: string): string {
  // analysis
  // ensure edge
  // ensure state transfer function
  // find best sub

  if (s.length === 1) {
    return s;
  }

  if (s.length === 2) {
  }
}
// @lc code=end
